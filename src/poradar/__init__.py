"""Psyop Radar: local-first influence-operation radar.

Design rule: a generative model (local Ollama) writes; Jev (TypeSafe System One)
decides bounded semantic questions; deterministic code owns permissions,
thresholds, and consequences.
"""

__version__ = "0.1.0"
